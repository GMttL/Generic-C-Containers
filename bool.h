#ifndef __bool__
#define __bool__

/**
 * Type: bool
 * ----------
 * Provides a lame but adequate stand-in for the bool we're used
 * to having around in C++. 
 */

typedef enum {
  false, true
} bool;

#endif
